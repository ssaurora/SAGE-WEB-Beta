# 前端 Phase 6 联调收口清单

**版本**：v1.0  
**日期**：2026-04-03  
**阶段目标**：在主链已完成的前提下，统一收口体验、契约与验收脚本

---

## 1. 统一契约层

- [ ] 固化 Results DTO → ViewModel 适配器边界。
- [ ] 固化 Scene Overview / Results / Governance 的返回字段边界。
- [ ] 明确 mock API 到真实后端的替换点。
- [ ] 汇总一份字段映射对照表，避免联调时页面层改动扩散。

## 2. 全链路走查

- [ ] Scene 创建 → Overview → Workbench → Task Governance → Results 全链路人工走查。
- [ ] 校验每个跳转是否携带上下文参数（from / taskId / resultId）。
- [ ] 校验回溯入口是否准确返回上游页面。
- [ ] 校验失败恢复路径是否能回到 Workbench。

## 3. 边界态统一

- [ ] 统一 loading 页面骨架样式。
- [ ] 统一 error 页面文案与返回按钮。
- [ ] 统一 empty state 文案与 CTA。
- [ ] 检查关键页面在空数据时是否仍可顺利导航。

## 4. 可发布性检查

- [ ] 重新执行 `npm run build` 并记录结果。
- [ ] 检查主要路由在本地 dev 模式下的页面表现。
- [ ] 如接入真实后端，优先只替换 `lib/api/*` 层。
- [ ] 补充最小联调说明，便于后续后端接手。

## 5. 建议执行顺序

1. 先做契约层收口。
2. 再做全链路走查。
3. 然后统一边界态与样式。
4. 最后补发布前检查。

---

## 6. 当前建议

当前最该做的是：

- 先完成契约与联调脚本收口
- 再做一次完整的手工端到端走查
- 最后进入真实后端替换

import { PlaceholderPage } from "@/components/pages/placeholder-page";

export default function TasksPage() {
  return (
    <PlaceholderPage
      title="Tasks"
      description="任务列表与筛选入口占位页。"
      bullets={["后续对接主状态、阶段状态与结果可达性。"]}
    />
  );
}

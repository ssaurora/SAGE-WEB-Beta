import { PlaceholderPage } from "@/components/pages/placeholder-page";

export default function SettingsPage() {
  return (
    <PlaceholderPage
      title="Settings"
      description="系统设置入口占位页。"
      bullets={["后续承接模式切换、权限与可见性配置。"]}
    />
  );
}
